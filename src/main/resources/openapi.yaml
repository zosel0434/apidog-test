openapi: 3.0.3
info:
  title: Spring Domain Filter Practice API
  version: 0.0.1-SNAPSHOT

paths:
  /api/orders:
    get:
      summary: 주문 목록 조회
      operationId: getOrders
      parameters:
        - name: orderNumber
          in: query
          required: false
          schema:
            type: string
        - name: orderStatus
          in: query
          required: false
          schema:
            $ref: '#/components/schemas/OrderStatus'
      responses:
        '200':
          description: 주문 목록
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/OrderResponse'

components:
  schemas:
    OrderStatus:
      type: string
      enum:
        - PENDING
        - CANCELED

    OrderResponse:
      type: object
      properties:
        orderNumber:
          type: string
        orderStatus:
          $ref: '#/components/schemas/OrderStatus'
